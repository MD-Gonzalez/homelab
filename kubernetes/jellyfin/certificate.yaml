apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: tv-sigilos-st-tls
  namespace: homelab
spec:
  secretName: tv-sigilos-st-tls
  issuerRef:
    name: letsencrypt-prod
    kind: ClusterIssuer
  dnsNames:
  - tv.sigilos.st
