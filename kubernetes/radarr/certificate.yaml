apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: radarr-sigilos-st-tls
  namespace: homelab
spec:
  secretName: radarr-sigilos-st-tls
  issuerRef:
    name: letsencrypt-prod
    kind: ClusterIssuer
  dnsNames:
  - radarr.sigilos.st
