apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: docs-sigilos-st-tls
  namespace: homelab
spec:
  secretName: docs-sigilos-st-tls
  issuerRef:
    name: letsencrypt-prod
    kind: ClusterIssuer
  dnsNames:
  - docs.sigilos.st
