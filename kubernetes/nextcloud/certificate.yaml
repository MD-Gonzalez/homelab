apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: cloud-sigilos-st-tls
  namespace: homelab
spec:
  secretName: cloud-sigilos-st-tls
  issuerRef:
    name: letsencrypt-prod
    kind: ClusterIssuer
  dnsNames:
  - cloud.sigilos.st
