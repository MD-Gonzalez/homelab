apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: ntfy-sigilos-st-tls
  namespace: homelab
spec:
  secretName: ntfy-sigilos-st-tls
  issuerRef:
    name: letsencrypt-prod
    kind: ClusterIssuer
  dnsNames:
  - ntfy.sigilos.st
