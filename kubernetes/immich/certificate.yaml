apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: pic-sigilos-st-tls
  namespace: homelab
spec:
  secretName: pic-sigilos-st-tls
  issuerRef:
    name: letsencrypt-prod
    kind: ClusterIssuer
  dnsNames:
  - pic.sigilos.st
