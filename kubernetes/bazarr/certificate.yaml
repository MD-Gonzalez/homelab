apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: bazarr-sigilos-st-tls
  namespace: homelab
spec:
  secretName: bazarr-sigilos-st-tls
  issuerRef:
    name: letsencrypt-prod
    kind: ClusterIssuer
  dnsNames:
  - bazarr.sigilos.st
